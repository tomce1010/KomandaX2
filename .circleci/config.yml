version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14.17.0-browsers
    working_directory: /KomandaX2/.circleci
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  version: 2.1
  build-and-test:
    jobs:
      - build