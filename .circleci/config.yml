version: 2.1

jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run:
          name: Build
          command: |
            javac -d build/classes src/fxControllers/CreateCargo.java
          working_directory: ~/KomandaX2
      - run:
          name: Test
          command: |
            java -cp build/classes com.example.MyClassTest
          working_directory: ~/KomandaX2

workflows:
  version: 2.1
  build-and-test:
    jobs:
      - build